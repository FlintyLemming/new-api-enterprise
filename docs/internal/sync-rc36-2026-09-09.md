# 2026-09-09 同步上游 v1.0.0-rc.36

## 基线和历史核对

- 更新前：`internal-custom` / `d921bc998`，已包含 rc.30（`27ff6a876`）。备份分支 `backup/internal-custom-before-rc36-20260909`。
- 目标：上游 `v1.0.0-rc.36` / `ea7cb0ba4`。从 `upstream=https://github.com/QuantumNous/new-api.git` 抓取 tag，没有跟随 tag 之后的 main。
- 合并：`a72e8c116`，两个父提交分别为 `d921bc998` 和 `ea7cb0ba4`。不重写内部历史，本地 `main` 从 rc.30 快进至 rc.36。
- `origin` 是 `git.mitsea.com:FlintyLemming/new-api.git`，`upstream` 才是 QuantumNous/new-api。总台账原先写反，现已更正。
- 用 `git log --no-merges HEAD --not v1.0.0-rc.36` 在合并前核对了 83 个内部提交，结合 `git diff v1.0.0-rc.30 HEAD` 和每项详情检查实际残留。
- `feat/exchange-key`、`feature/langfuse-tracing`、`fix/log-duration-ms`、`fix/oai-to-claude-tool-call-stream`、`list` 均为当前历史祖先。远端 `origin/feat/strip-claude-system-prefix`、`origin/feature/user-concurrent-ip-limit` 也已在当前历史中；没有遗漏尚未合入的这些分支提交。
- `list` 的并发额度修复 `ccd535ef8` 已是上游祖先，不是待携带的内部 patch。耗时分支的 `d00e620f0` 与内部 cherry-pick `d535b112d` 是同一项功能，不重复应用。
- 原工作区未跟踪的 `postgres-mysql-migration-legacy-objects.md` 原样保留，未纳入提交。现存 stash 不变。本次未推送或部署。

## 补丁决定

| 补丁 | 决定 | 依据与适配 |
| --- | --- | --- |
| Anthropic Messages cache usage | 归档 | #7137 / #7170 的共享 Claude usage 转换默认扣除 cache，并保留原始 sidecar；终态修正首帧估算。删除内部开关、选项、表单和翻译。旧 JSON 字段忽略，不改存量数据。 |
| OAI→Claude tool stream | 归档 | 上游按 ID/index 跟踪工具，以 `Started` 去重，先发内容再处理 finish。删除内部状态机副本，保留 3 个行为回归测试。 |
| Langfuse | 保留 | 上游无 OTLP/session 导出替代。保留生命周期、计费快照和 `cache_prompt_token_semantic`；后者是原始计费用量声明，与归档的客户端回包开关不同。OTel 维持 1.44.0。 |
| Strip Claude billing header | 保留 | 上游无等价过滤。适配 `strings.Builder`，保留 nil 检查、过滤为空的行为及 OpenRouter Claude 例外。 |
| Exchange key | 保留 | 上游无免 token 行的 HMAC 身份。鉴权、账户额度扣减、日志用户视图和私密配置守卫均保留。 |
| User concurrent IP limit | 保留 | 上游无等价用户/IP 滑动窗口。保留用户列和 v3 缓存，以及 relay/video/插件 TokenAuth 链。 |
| Log duration ms | 保留 | 上游仍存整秒。改用 `LogOther.SetPublic` 写入 `duration_ms`；移动卡片在新的 `common-log-mobile-card.tsx` 接入耗时与 TPS。 |
| Async refund used_quota backport | 继续归档 | 原 rc.30 归档结论不变，不重新携带副本。 |

生产文件 `relaykit/relayconvert/convmeta/meta.go` 和 `relaykit/relayconvert/internal/oai_chat/to_claude_messages_resp.go` 与 rc.36 完全一致。缓存开关关闭/开启差异及强制首帧置零不再保留，采用上游统一协议语义。

合并同时保留上游日志权限隔离、安全认证、显式模型修饰符、工具附加计费和 schema 规范化。修复自动合并的双 updates map，保留上游密码校验与认证版本流程；新增价格测试传入内部四参数计费接口。封禁用户测试显式初始化 i18n，不依赖其他测试的执行顺序。

## 验证结果

| 命令（仓库根目录，另有注明除外） | 结果 |
| --- | --- |
| `go test ./...` | 通过；不将未配置 DSN 时跳过的集成测试计为数据库验证 |
| `go build ./...` | 通过 |
| `cd relaykit && GOWORK=off go test ./...` | 通过，含原工具流三个回归 |
| `cd relaykit && GOWORK=off go build ./...` | 通过，独立模块未依赖宿主 |
| `go test -race ./service/langfuse/...` | 通过（313.036s） |
| `cd web && bun install --frozen-lockfile` | 通过，lockfile 不变 |
| `cd web && bun run test` | 121 文件、1089 测试通过（新增移动回归前） |
| `cd web && bun run test src/features/usage-logs/components/__tests__/mobile-card.test.tsx` | 新增后 14/14 通过；其中 2 项分别保护毫秒优先与旧秒数回退。上游原组件复现 1 fail / 13 pass，移植后 14 pass |
| `cd web && bun run typecheck` | 通过 |
| `cd web && bun run build` | 通过，产物约 58.6 MB / gzip 16.9 MB；存在上游的大 chunk 提示 |
| `cd web && bun run i18n:sync` | 七语言 missingCount=0；保留的 `Exchange Key` 技术名称在部分语言被报告为同英文，非缺翻译 |
| `git diff --check` / `git diff --cached --check` | 通过 |

前端手工修改的六个文件执行 `web/node_modules/.bin/oxlint -c .oxlintrc.json <files>` 无错误：渠道 drawer、channel-form、channel-form-errors、channels/types、common-log-mobile-card 及其 mobile-card 测试。这六个文件用 oxfmt 格式化，版权头原样保留。

全仓 `bun run lint` **未通过**：与隔离的原始 rc.36 使用相同 oxlint/config 比较，忽略行号后的 221 条 error 完全相同，没有新增的内部 error。没有为同步基线扩散修改上游已有 lint 问题。最初的 root build、旧 i18n 断言和移动红测失败均已修复并重跑，不作为最终通过结果。

## 数据库矩阵

只使用本机新建的 `newapi-rc36-review-*` 容器和 `/tmp/new-api-rc36-review` SQLite 文件。没有连接现有生产数据库，未执行生产库说明中的清理 SQL。

| 引擎 | 实测版本 | 新建内部版 | rc.30 内部版升级 | rc.36 上游版升级 | 分离日志库、重复启动 |
| --- | --- | --- | --- | --- | --- |
| SQLite | 3.50.4（实际 Go driver 查询） | 通过 | 通过 | 通过 | 通过 |
| MySQL | 8.4.5（本地 `mysql:8`） | 通过 | 通过 | 通过 | 通过 |
| MySQL | 5.7.44 | 通过 | 通过 | 通过 | 通过 |
| PostgreSQL | 15.17 | 通过 | 通过 | 通过 | 通过 |
| PostgreSQL | 9.6.24 | 通过 | 通过 | 通过 | 通过 |

每一格均由对应版本编译的程序实际调用 `model.InitDB` / `model.InitLogDB`，先创建代表性数据，再以更新后的内部版连续启动/迁移两次。旧版种子使用更新前的 `d921bc998`，上游种子使用目标发布 tag `ea7cb0ba4`。检查用户额度和中文、token 额度、渠道 JSON、prefill JSON、decimal 价格、option 和分离日志库记录保留；检查用户名、token key、prefill name 的重复插入仍被拒；比对两次迁移后的完整 schema 元数据一致，并检查原有索引/约束仍在。

此外 `TestMigrationSchemaStability` 在 SQLite/MySQL 8.4.5/PostgreSQL 15.17 以及 MySQL 5.7.44/PostgreSQL 9.6.24 上实际执行，无 DSN 跳过。覆盖 CHAR 长度真变更、唯一约束新增/删除、MySQL decimal 精度/default/null 变化及重复 AutoMigrate 无 DDL。

GORM 组采用 rc.36 的版本：core 1.25.12、mysql driver 1.5.7、postgres driver 1.5.9、glebarez/sqlite 1.11.0。上游 go.mod 分别要求 GORM 1.25.7、1.25.10、1.25.7，兼容性判断结合实际矩阵，不只依赖版本号：
[MySQL go.mod](https://raw.githubusercontent.com/go-gorm/mysql/v1.5.7/go.mod)、[PostgreSQL go.mod](https://raw.githubusercontent.com/go-gorm/postgres/v1.5.9/go.mod)、[SQLite go.mod](https://raw.githubusercontent.com/glebarez/sqlite/v1.11.0/go.mod)。

限制：尝试 `docker pull mysql:5.7.8` 被 Docker 29.4.0 拒绝（`unsupported manifest media type ... v1+prettyjws`），因此精确到 5.7.8 的验证未完成，不能声称该最小 patch 版本已经验证；已覆盖 5.7 系列的 5.7.44。PostgreSQL 覆盖 9.6 系列的 9.6.24。真实 Langfuse 服务 E2E/生产流量没有在本次重新执行，已有部署验收不冒充本次结果。

### 复现命令和留档

辅助程序和驱动脚本存于 [verification/rc36](verification/rc36/)。`.go.txt` 为验证源码，不参与产品编译。以下命令只用于新建测试环境，容器名/数据库名已存在时应使用新的隔离目录和名称，不要指向生产 DSN。

```bash
mkdir -p /tmp/new-api-rc36-review
cp docs/internal/verification/rc36/dbcheck.go.txt /tmp/new-api-rc36-review/dbcheck.go
cp docs/internal/verification/rc36/run-matrix.py /tmp/new-api-rc36-review/run-matrix.py
git worktree add --detach /tmp/new-api-rc36-review/base-rc30 d921bc998
git worktree add --detach /tmp/new-api-rc36-review/upstream-rc36 v1.0.0-rc.36
go build -o /tmp/new-api-rc36-review/check-custom /tmp/new-api-rc36-review/dbcheck.go
(cd /tmp/new-api-rc36-review/base-rc30 && go build -o /tmp/new-api-rc36-review/check-rc30 /tmp/new-api-rc36-review/dbcheck.go)
(cd /tmp/new-api-rc36-review/upstream-rc36 && go build -o /tmp/new-api-rc36-review/check-upstream /tmp/new-api-rc36-review/dbcheck.go)
docker run -d --name newapi-rc36-review-mysql -e MYSQL_ROOT_PASSWORD=rc36-test-only -p 127.0.0.1:23306:3306 mysql:8
docker run -d --name newapi-rc36-review-mysql-min -e MYSQL_ROOT_PASSWORD=rc36-test-only -p 127.0.0.1:23307:3306 mysql:5.7.44
docker run -d --name newapi-rc36-review-postgres -e POSTGRES_PASSWORD=rc36-test-only -p 127.0.0.1:25432:5432 postgres:15
docker run -d --name newapi-rc36-review-postgres-min -e POSTGRES_PASSWORD=rc36-test-only -p 127.0.0.1:25433:5432 postgres:9.6.24
# 确认各新容器 ready 后执行：
python3 /tmp/new-api-rc36-review/run-matrix.py
MYSQL57=1 python3 /tmp/new-api-rc36-review/run-matrix.py
TEST_MYSQL_DSN='root:rc36-test-only@tcp(127.0.0.1:23306)/rc36_v2_fresh_main?charset=utf8mb4&parseTime=true' TEST_POSTGRES_DSN='postgres://postgres:rc36-test-only@127.0.0.1:25432/rc36_v2_fresh_main?sslmode=disable' go test ./model -run '^TestMigrationSchemaStability$' -count=1 -v
TEST_MYSQL_DSN='root:rc36-test-only@tcp(127.0.0.1:23307)/rc36_v2_fresh_main?charset=utf8mb4&parseTime=true' TEST_POSTGRES_DSN='postgres://postgres:rc36-test-only@127.0.0.1:25433/rc36_v2_fresh_main?sslmode=disable' go test ./model -run '^TestMigrationSchemaStability$' -count=1 -v
```

本次原始日志、schema 快照和三个验证程序保留在 `/tmp/new-api-rc36-review/`。MySQL 首轮失败来自辅助程序将保留字 `key` 写进裸 Where，改成 GORM 结构体条件后用新测试库重跑全部 MySQL 路径通过；产品代码未因该辅助脚本失败作修改。该目录为临时证据，源码和最终结果已在本台账持久留档。

测试结束后已删除四个专用容器及其匿名卷，并移除两个只读验证 worktree；原始日志/schema 快照仍保留在临时目录，更新前的 Git 备份分支继续保留。实测 schema 测试的 MySQL 8 DSN 使用 `rc36_fresh_main`，MySQL 5.7 使用 `rc36_v2_fresh_main`，两版 PostgreSQL 使用 `rc36_fresh_main`；上面的复现脚本统一创建 `rc36_v2_*` 库以避免初次辅助程序失败遗留的测试库重名。
